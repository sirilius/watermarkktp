<div class="flex flex-col items-center mt-20 md:mt-32">
  <h2 class="text-3xl text-center font-bold mb-12 tracking-tight">
    Tanggapan Orang-Orang
  </h2>

  <!-- Testimonials Grid -->
  <div class="md:columns-2 lg:columns-3 gap-6 space-y-6 w-full max-w-6xl px-4">
    {% for testimonial in testimonials %}
    <div
      class="border border-gray-200 rounded-2xl shadow-sm hover:shadow-md transition-shadow duration-300 p-5 bg-white break-inside-avoid"
    >
      <a
        href="{{ testimonial.url }}"
        target="_blank"
        rel="noopener noreferrer"
        data-umami-event="testi-Twitter-{{ testimonial.username }}"
        class="flex items-center mb-4 space-x-3"
      >
        {% image "./src/img/testi-pp-" + testimonial.username + ".jpg",
        testimonial.name + " profile photo", [64, 64], "rounded-full w-12
        object-cover" %}

        <div class="flex justify-between w-full items-center">
          <div>
            <p class="font-semibold text-gray-900">{{ testimonial.name }}</p>
            <p class="text-gray-500 text-xs">{{ testimonial.profession }}</p>
          </div>
          <i
            class="ri-{{ testimonial.icon }} {% if testimonial.icon == 'youtube-fill' %} text-[#FF0000] text-3xl {% endif %} text-2xl"
          ></i>
        </div>
      </a>

      <p class="text-gray-700 leading-relaxed">
        {{ testimonial.content | safe }}
      </p>
    </div>
    {% endfor %}
  </div>

  <!-- Highlighted Press Quote -->
  <div class="max-w-3xl text-center mt-16 px-6">
    <blockquote class="text-2xl italic text-gray-800 leading-relaxed mb-6">
      “Watermark KTP bikinan Kevin benar-benar membantu. Setidaknya untuk kita
      bisa
      <span class="bg-[#bce954] text-black px-2 rounded-md">
        mencegah penyalahgunaan data di KTP.
      </span>”
    </blockquote>
    <a
      href="https://mojok.co/konter/watermark-ktp-solusi-baru-memerangi-pinjol-laknat/"
      target="_blank"
      rel="noopener noreferrer"
      class="inline-block hover:opacity-90 transition duration-300"
      data-umami-event="testi-Mojok"
    >
      {% image "./src/img/logo-mojok.png", "Logo Mojok.co", [163, 60], "w-36 bg-black px-6 py-2 rounded-md" %}
    </a>
  </div>
</div>